export type Workbook = {
  name: string;
  _id: string;
  createdBy: string;
  _creationTime: number;
};

export type WorkbookTableRow = {
  contactName: string;
  companyUrl: string;
  workEmail: string;
};
